---
description: Define the core entities and relationships
---

1. Ask user for key entities.
2. Create/Update `product/data-model/data-model.md` with:

   ```markdown
   # Data Model

   ## [Entity Name]

   - field: type
   - field: type
   ```

3. Notify user.
