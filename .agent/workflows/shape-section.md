---
description: Define a section's scope and requirements
---

1. Ask user which section from the Roadmap to work on.
2. Create directory `product/sections/[section-slug]/`.
3. Create `product/sections/[section-slug]/spec.md` with:
   ```markdown
   # [Section Title]

   ## Goals

   ...

   ## Requirements

   ...
   ```
4. Notify user.
